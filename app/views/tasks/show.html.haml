.content 
  .content_detail
    %h1.content_title= @task.title
    - if user_signed_in? && current_user.has_written_?(@task)
      .content_detail_actions
        .dropdown
          = image_tag 'actions.svg', class: 'dropbtn'
          .dropdown-content.mini
            = link_to '編集する', edit_board_task_path(@board,@task)
            = link_to '削除する', board_task_path(@board,@task), data: { method: 'delete', confirm: '本当に削除してもいいですか？' }
.content
  - if @task.eyecatch.attached?
    .content_image 
      = image_tag @task.eyecatch

.content 
  .content_text
    = @task.content

.content 
  %h2 comments
  - @comments.each do |comment|
    .content_comment 
      .comment_detail
        = image_tag comment.user.avatar_image
        %div
          %p= comment.content

  = link_to new_board_task_comment_path(board_id: @board.id, task_id: @task.id) do
    .card_newcomment 
      コメントを追加



  





